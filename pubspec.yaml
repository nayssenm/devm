name: devm_covoitlocal
description: A new Flutter project.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5
  firebase_core: ^2.12.0
  firebase_auth: ^4.6.0
  cloud_firestore: ^4.17.5
  firebase_storage: ^11.0.0
  google_maps_flutter: ^2.3.0
  geolocator: ^9.0.2
  equatable: ^2.0.7
  flutter_bloc: ^9.1.1
  get_it: ^9.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
